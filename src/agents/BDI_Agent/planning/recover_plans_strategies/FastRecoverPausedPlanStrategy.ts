// TODO if an intention is requested to be "injected" during the execution of a plan, the current plan should be paused and the new intention should be executed first.
//  After that, the paused plan should be resumed, IDENTIFYING A VALID POINT TO RESUME FROM.
//  The identification of existence-of-resume determines whether the injected intention is accepted or rejected.